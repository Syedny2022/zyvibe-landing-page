{
  "entities": {
    "Lead": {
      "title": "Lead",
      "description": "A potential customer who has submitted their contact information.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the lead."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the lead."
        },
        "source": {
          "type": "string",
          "description": "How the lead was captured (e.g., 'form', 'voice')."
        },
        "struggle": {
          "type": "string",
          "description": "The biggest creator struggle mentioned by the lead."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the lead was created."
        }
      },
      "required": ["email", "createdAt"]
    }
  },
  "firestore": {
    "/leads/{leadId}": {
      "schema": "Lead",
      "description": "Collection of leads captured from the landing page."
    }
  }
}
